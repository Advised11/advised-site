// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  site: 'https://www.advised-seo.fr',
  // Si vous déployez sur GitHub Pages dans un sous-dossier (ex: user.github.io/advised),
  // décommentez et adaptez la ligne suivante :
  // base: '/advised',
});
